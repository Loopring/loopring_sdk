{
  "name": "loopring-sdk",
  "version": "0.0.1",
  "author": "Loopring Dev Team",
  "description": "dexwebapp new version",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "npx honkit build",
    "serve": "npx honkit serve",
    "predeploy": "yarn build; sleep 1;",
    "deploy": "gh-pages -d _book"
  },
  "devDependencies": {
    "gh-pages": "^3.2.2",
    "honkit": "^3.6.20"
  },
  "dependencies": {
    "js-base64": "git+https://github.com/GameStop/js-base64.git"
  }
}
